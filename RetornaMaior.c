#Include <stdio.h>

int Maior(int x, int y);

int main()
{
	int n1, n2, n3;
	
	printf("Insira os dois valores para retornarmos qual é o maior: ")
	scanf("%d %d", &n1, &n2);
	
	maior = returnMaior(n1, n2);
	printf("O maior valor entre os dois é: %d", maior);
	
	return 0
}

int returnMaior(int x, int y)
{
	if (a > b)
		return a;
	else 
		return b;
}
